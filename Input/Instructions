The attached do file constructs the AIOE, AIIE, and AIGE with the input files included. 

The two compressed files containing datasets (one ZIP; one .7z) must be extracted. Then, set the data directory and run the do file to construct all datasets. 
